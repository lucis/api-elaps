type Query {
  cliente(id: Id): Cliente
  clientes(
    query: String = "",
    orderBy: String = "OrderByPriceDESC",
    from: Int = 0,
    to: Int = 9
  ): [Cliente]
}

type Mutation {
  # Aparentemente há de ser criado um tipo ClienteInput para isso, depois testo se pode funcionar com cliente
  addCliente(): Cliente
}