type Peca {
    id: String!
    referencia: String!
    descricao: String!
    preco: Int!
    unidade: String!
    ultimaMudancaPreco: Int
}

input PecaInput {
    referencia: String!
    descricao: String!
    preco: Int!
    unidade: String!
}