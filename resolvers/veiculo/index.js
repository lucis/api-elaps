const queries = require('./veiculoQueries');
const rootResolvers = require('./veiculoRootResolvers');
const mutations = require('./veiculoMutations');

module.exports = {
    queries,
    rootResolvers,
    mutations
};