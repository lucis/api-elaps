const queries = require('./servicoQueries');
const rootResolvers = require('./servicoRootResolvers');
const mutations = require('./servicoMutations');

module.exports = {
    queries,
    rootResolvers,
    mutations
};