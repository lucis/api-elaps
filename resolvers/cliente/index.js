const queries = require('./clienteQueries');
const rootResolvers = require('./clienteRootResolvers');
const mutations = require('./clienteMutations');

module.exports = {
    queries,
    rootResolvers,
    mutations
};